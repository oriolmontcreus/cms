import type { PageConfig } from '../types';

export const config: PageConfig = {
  "title": "Test21",
  "slug": "test21",
  "fields": [
    {
      "type": "text",
      "name": "title",
      "label": "Títol",
      "required": true,
      "placeholder": ""
    }
  ]
};
